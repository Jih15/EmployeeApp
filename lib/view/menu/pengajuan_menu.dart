import 'package:flutter/material.dart';

class PengajuanMenu extends StatelessWidget {
  const PengajuanMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pengajuan Page'),
        centerTitle: true,
      ),
    );
  }
}
